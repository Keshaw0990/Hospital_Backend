package com.hospital.hospital.repo;

import com.hospital.hospital.entity.TbFormMaster;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FormMasterRepository extends JpaRepository<TbFormMaster, Long> {
}
